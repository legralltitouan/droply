<Window x:Class="Droply.IncomingFileToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Width="380" Height="130"
        WindowStyle="None" AllowsTransparency="True"
        Background="{x:Null}"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False" ResizeMode="NoResize">
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </Window.Style>
    <Border CornerRadius="8"
            Background="{DynamicResource SolidBackgroundFillColorTertiaryBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
        <Border.RenderTransform>
            <TranslateTransform x:Name="SlideXf" X="-80"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" ShadowDepth="6" Opacity="0.35" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{DynamicResource AccentFillColorDefaultBrush}"
                    CornerRadius="8,0,0,8"/>
            <Grid Grid.Column="1" Margin="14,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <ui:SymbolIcon Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Symbol="CloudArrowDown24"
                               FontSize="22" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                               VerticalAlignment="Top" Margin="0,2,12,0"/>
                <TextBlock Grid.Column="1" Grid.Row="0" Text="{DynamicResource L.Toast.Title}"
                           FontWeight="SemiBold" FontSize="13"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{DynamicResource L.Toast.Subtitle}"
                           FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap" Margin="0,2,0,8"/>
                <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button x:Name="IgnoreBtn" Content="{DynamicResource L.Toast.Ignore}"
                               Appearance="Secondary" MinWidth="80" Height="28"
                               Margin="0,0,8,0" Click="Ignore_Click"/>
                    <ui:Button x:Name="DownloadBtn" Content="{DynamicResource L.Toast.Download}"
                               Appearance="Primary" Icon="{ui:SymbolIcon ArrowDownload24}"
                               MinWidth="110" Height="28" Click="Download_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>